{
  "name": "narratore-di-sogni-root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build:all": "npm --workspace backend run build && npm --workspace frontend run build"
  }
}
